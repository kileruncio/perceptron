from Perceptron import Perceptron

file = input('Enter name of a file: ')
precision = input('Enter value of fdgdfg: ')

perceptron = Perceptron(file, precision)

print(f'Go to {file} with value {precision}')